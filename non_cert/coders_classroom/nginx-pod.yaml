apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: non-certs
  labels:
    createdBy: bright
spec:
  containers:
    - name: nginx-pod
      image: nginx
      ports:
        - containerPort: 8080 
